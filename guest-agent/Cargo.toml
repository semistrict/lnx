[package]
name = "lnx-agent"
version = "0.3.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
lnx-protocol = { path = "../lnx-protocol" }
postcard = { version = "1.1", features = ["alloc"] }
serde = { version = "1.0", features = ["derive"] }
