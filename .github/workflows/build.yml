name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Check formatting
        run: test -z "$(gofmt -l . | grep -v third_party/)" || (gofmt -l . | grep -v third_party/ && exit 1)

      - name: Build guest init (linux/arm64)
        run: CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o cmd/lnx/init ./cmd/init

      - name: Vet (Linux-compatible packages)
        run: go vet ./internal/... ./cmd/init/...

      - name: Run tests (Linux-compatible packages)
        run: go test -race -v ./internal/...

  build-macos:
    runs-on: macos-latest
    needs: check
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build guest init (linux/arm64)
        run: CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o cmd/lnx/init ./cmd/init

      - name: Vet
        run: go vet ./...

      - name: Build lnx (macOS)
        run: go build -ldflags '-extldflags "-Wl,-no_warn_duplicate_libraries"' -o lnx ./cmd/lnx

      - name: Run tests
        run: go test -race -v ./...

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: lnx-darwin-arm64
          path: lnx
